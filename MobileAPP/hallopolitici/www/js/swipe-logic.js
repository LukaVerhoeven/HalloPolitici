// Global variables
var _CURRENT_STEP = 1;
var _NAV_BAR = document.querySelector('nav');

const swiping = {
    initialize: function () {
        _CURRENT_STEP = 1; // double check on current step set to 1
        swiping.bindEvents();
    },
    bindEvents: function () {
        
    },
    swipeCall: function () {

    }
};

swiping.initialize();
